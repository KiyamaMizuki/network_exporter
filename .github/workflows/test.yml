name: test

on:
  push

jobs:
  curl_test:
    runs-on: ubuntu-latest 
    steps:
    - name: test
      run: |
        echo ${{env.TEST_URL}}
        echo ${{vars.secrets.TEST_URL}}
        curl ${{secrets.TEST_URL}}
